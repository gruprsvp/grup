/// Enum for all tables in the database.
enum Tables {
  roles,
  profiles,
  groups,
  members,
  invites,
  schedules,
  default_replies,
  replies,
}

/// Enum with all bucket IDs in Supabase.
enum Buckets {
  public,
  private,
}
